# Pinned for reproducible production builds. Bump deliberately, then re-test.
fastapi==0.137.2
uvicorn[standard]==0.49.0
# Gemini one-call turn (services.py). Every LLM call — the safety classifier and
# the Aira reply — runs on this. There is no second provider to fall back to.
google-genai==2.10.0
httpx==0.28.1
python-dotenv==1.2.2
python-multipart==0.0.32
# Google Sign-In (Android/web): verifies the ID token's signature against
# Google's JWKS and checks aud/iss/exp (accounts.py, device_auth.py).
pyjwt[crypto]==2.13.0
# Multi-worker rate limiting (security.py imports it lazily when REDIS_URL is
# set — without this pin that import crashes in production).
redis==8.0.1
# Error tracking — inert until SENTRY_DSN is set (app.py).
sentry-sdk[fastapi]==2.64.0
# Postgres backend — only needed when DATABASE_URL is set; SQLite is the default.
# psycopg[binary]>=3.1
