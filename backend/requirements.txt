# Pinned for reproducible builds. Bump deliberately, then re-test.
fastapi==0.137.2
uvicorn[standard]==0.49.0
# Gemini: every LLM call — chat, the safety classifier, and multimodal audio
# turns. There is no fallback provider by design.
google-genai==2.10.0
httpx==0.28.1
python-dotenv==1.2.2
python-multipart==0.0.32
pyjwt[crypto]==2.13.0
# Multi-worker rate limiting (security.py imports it lazily when REDIS_URL is
# set — without this pin that import crashes in production).
redis==8.0.1
# Error tracking — inert until SENTRY_DSN is set.
sentry-sdk[fastapi]==2.64.0

# Postgres — required in production (see config.verify_production_config).
# Uncomment when DATABASE_URL is provisioned; SQLite is the dev default.
# psycopg[binary]>=3.1

# ── Dev / test ───────────────────────────────────────────────────────────────
pytest==8.4.2
pytest-asyncio==1.2.0
