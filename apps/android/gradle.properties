android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
android.defaults.buildfeatures.resvalues=false
android.defaults.buildfeatures.shaders=false

# Aira backend base URL (BuildConfig.AIRA_API_BASE). Defaults to 10.0.2.2:8000
# (host loopback from the emulator). Uncomment / override for a real backend,
# or pass -PairaApiBase=... on the Gradle command line.
# airaApiBase=https://api.aira.app
