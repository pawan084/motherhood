<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only manifest overlay. The merger folds this attribute into the main
  <application>; release builds get no networkSecurityConfig at all and so keep
  Android's default of blocking cleartext HTTP entirely.

  This exists purely so the documented dev backend (http://10.0.2.2:8000) is
  reachable from the emulator — see res/xml/network_security_config.xml.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application android:networkSecurityConfig="@xml/network_security_config" />

</manifest>
